# Axiom Cloud CLI

A lightweight AWS CLI alternative for fast cloud operations.

## Quick Setup

```bash
pip install -e .
axiom init
```

If you get a "missing dependency" error, run the auto-fixer:

```bash
python setup.py auto-fix
```

## Usage

```bash
axiom list-instances
axiom deploy --stack prod
```
